<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="window_bg">#0b0b0b</color>
    <!-- The app's own ground, so the icon reads as the same product the
         web app is rather than a differently-coloured sibling (which is
         what the amber admin icon is deliberately for). -->
    <color name="ic_launcher_background">#0b0b0b</color>
</resources>
